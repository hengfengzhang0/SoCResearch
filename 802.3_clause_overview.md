### IEEE 802.3 协议栈全景维度拆解矩阵 (纯硬件/芯片视角)

| 协议子层 | 典型与最新核心 Clause (规范条款) | 最新更新时间与来源 | 各 Clause 核心内容详解 (它具体规定了什么？) | 总速率与物理 Lane 架构拆解 | 最新修订应对的核心应用场景 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **MAC**<br>*(介质访问控制)* | **Clause 4** (MAC 基础)<br>**Clause 169** (800G 扩展) | **2024年2月**<br>*(802.3df)* | **Clause 4**: 定义恒定不变的以太网帧格式（DA/SA/Type/FCS）、纯逻辑状态机。<br>**Clause 169**: 专门针对 800G 定义 MAC 控制参数，例如帧间隙 (Deficit Idle Count)、最大吞吐量限制和延迟约束。 | **总速率**: 800 Gbps。<br>*(MAC 层不感知物理 Lane，它只把数据看作一个单一的、高达 800G 吞吐的逻辑管道)*。 | **AI 集群主干网络**：解决多 GPU 节点间海量参数同步时，MAC 层如何高效聚合吞吐而不产生逻辑瓶颈。 |
| **RS**<br>*(协调子层 / xMII)* | **Clause 117** (400GMII)<br>**Clause 170** (800GMII) | **2024年2月**<br>*(802.3df)* | **Clause 170**: 纯逻辑边界。定义了 800GMII 接口的数据通路位宽（通常为极其宽泛的并行总线，如多组 64-bit）和控制信号 (TXC/RXC)，用于隔离 MAC 时钟域与极高频的 PHY 数字时钟域。 | **逻辑承载**: 800 Gbps (内部通常拆分为并行总线，以降低 SoC 内部数字逻辑的布线频率)。 | **SoC 内部架构设计**：指导数字逻辑设计团队如何划分 MAC IP 与外挂/内置 PHY IP 之间的数据交互总线 (Data Path)。 |
| **PCS**<br>*(物理编码)* | **Clause 119** (400G)<br>**Clause 172** (800G) | **2024年2月**<br>*(802.3df)* | **Clause 172**: 数字前端的“重灾区”。它规定了：<br>1. **转码**: 将 64b/66b 转为 256b/257b 降低开销。<br>2. **FEC**: 强制实施极高强度的 **RS(544, 514) 前向纠错**。<br>3. **分发**: 插入对齐标记 (Alignment Marker)，并将纠错后的符号均匀分布到 8 条独立逻辑通道中。 | **向下分流映射**：<br>总速率 **800 Gbps**，在此被强制切分为 **8 条逻辑通道**，每条承载 **100 Gbps** 数据流。 | **大模型训练 (LLM)**：PAM4 调制在 100G/Lane 下误码率极高，必须依靠 Clause 172 定义的硬核 DSP/FEC 算法进行强行纠错，这是降低 AI 集群长尾延迟的关键。 |
| **PMA**<br>*(物理附加)* | **Clause 120** (400G)<br>**Clause 173** (800G) | **2024年2月**<br>*(802.3df)* | **Clause 173**: 混合信号的核心。规定了如何将 PCS 传来的比特流映射为 **PAM4 (四电平) 符号**，执行格雷码映射 (Gray coding)，以及串行化/解串 (SerDes)。它还定义了 PMA 层的时钟重定时 (Retimer) 和环回测试机制。 | **硬件 SerDes 约束**：<br>严格定义 **8 条物理通道 (8 Lanes)**，每条通道通过 **53.125 GBd 波特率的 PAM4** 信号实现 **100 Gbps** 的物理吞吐。 | **CPO (共封装光学) 与 Retimer**：指导高速交换机内部如何进行信号的中继和重定时，以克服极高频模拟信号在 PCB 上的衰减。 |
| **PMD**<br>*(物理介质)* | **Clause 162** (CR 铜缆)<br>**Clause 163** (KR 背板)<br>**Clause 167** (SR 光口) | **2022年-2024年**<br>*(802.3ck, 802.3db, 802.3df)* | 这是模拟电路的物理极限。<br>**Clause 162/163**: 定义了针对电接口的发射端电压摆幅、发送端预加重 (FFE)、接收端均衡 (CTLE/DFE) 以及极度严苛的信道插损 (Insertion Loss) 容限。<br>**Clause 167**: 定义了 VCSEL 激光器的眼图掩膜、光功率发射/接收下限。 | **物理连线**：<br>例如 800GBASE-CR8，后缀 "8" 即代表使用 **8 对差分收发线缆 (Lanes)**，每对跑 **100 Gbps (PAM4)**，最终合成 800 Gbps。 | **数据中心机架组网**：<br>1. **CR8 (DAC 铜缆)** 应对同机架内 GPU 节点到 Top-of-Rack 交换机极低成本、低延迟的直连。<br>2. **KR8 (背板)** 应对刀片服务器内部高速互联。 |
