### 协议栈全景维度表 (含最新更新日期与物理 Lane 拆解)

| 协议层级 (OSI映射) | 核心规范 (RFC / Clause) | 最新更新日期 (文件来源) | 最新更新的主要内容 (含总速率与 Lane 物理拆解) | 本质功能与物理实现形态 |
| :--- | :--- | :--- | :--- | :--- |
| **TCP**<br>*(L4 传输层)* | **RFC 9293** | **2022年8月**<br>*(IETF)* | **大一统重构**：整合过去的 TCP 补丁，形成单一现代化基础规范。*(注：TCP 层完全解耦，不感知底层物理带宽与 Lane 数量)* | **功能**：端到端可靠字节流、拥塞控制。<br>**形态**：OSI L4。系统内核协议栈 / TOE 硬件卸载引擎。 |
| **IP**<br>*(L3 网络层)* | **RFC 8200** (IPv6) | **2017年7月**<br>*(IETF)* | **IPv6 最终定稿**：严格界定了扩展报头的处理逻辑。*(注：网络层解耦，不感知物理带宽与 Lane)* | **功能**：全网逻辑寻址、分片重组。<br>**形态**：OSI L3。系统内核 / 路由器 ASIC。 |
| **MAC**<br>*(L2 链路层)* | **Clause 4, 80, 169** | **2024年2月**<br>*(IEEE 802.3df)* | **800G MAC 聚合**：扩展了 MAC 参数以支持高达 **800 Gbps 总速率** 的极致吞吐。*(此层将 100G/Lane × 8 Lanes 视为一个单一的 800G 逻辑管道)*。 | **功能**：组帧解帧、寻址、FCS 校验、流控。<br>**形态**：SoC 内部纯数字逻辑 MAC 控制器 IP。 |
| **RS**<br>*(L1/L2 协调)* | **Clause 81, 170** | **2024年2月**<br>*(IEEE 802.3df)* | **接口位宽翻倍**：定义 800GMII 接口，安全隔离 **800 Gbps (100 Gbps/Lane × 8 Lanes 支撑的逻辑带宽)** 的 MAC 时钟域与底层 PHY 时钟域。 | **功能**：将串行 MAC 映射为并行数据总线。<br>**形态**：SoC 内部数字前端的数据边界 (Data Path)。 |
| **PCS**<br>*(L1 编码层)* | **Clause 119, 172** | **2024年2月**<br>*(IEEE 802.3df)* | **重构纠错与分发逻辑**：针对 **800 Gbps 总带宽**，定义了严格的 **8 Lanes** 分发机制，并对每条 **100 Gbps/Lane** 的数据流执行高强度的 RS-FEC 对齐与纠错。 | **功能**：编码、加扰、通道分发 (MLD)、前向纠错。<br>**形态**：极耗面积与功耗的数字前端 (DSP/FEC) 逻辑。 |
| **PMA**<br>*(L1 物理附加)* | **Clause 120, 173** | **2024年2月**<br>*(IEEE 802.3df)* | **多路复用升级**：定义了串并转换与重定时(Retimer)规范，严格执行物理层的 **8 Lanes** 并行传输，每条 Lane 的物理吞吐为 **100 Gbps (实质为 53.125 GBd PAM4)**。 | **功能**：SerDes 串行化/解串、时钟数据恢复 (CDR)。<br>**形态**：极高频的混合信号/模拟电路宏单元。 |
| **PMD**<br>*(L1 物理介质)* | **Clause 162, 167** | **2024年2月**<br>*(IEEE 802.3df)* | **榨干物理极限**：全面标准化了总速率为 **800 Gbps** 的物理形态，明确规定通过 **8 条电气/光学物理通道 (8 Lanes)** 实现，每条通道承载 **100 Gbps** 的硬核指标 (如 800GBASE-CR8/SR8 中的 "8" 即代表 8 Lanes)。 | **功能**：驱动电平、接收端模拟均衡 (CTLE/DFE)。<br>**形态**：底层的光学收发器、DAC 线缆或 PCB 板级走线。 |
